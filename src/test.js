import LeftRightButton from './components/LeftRightButton'
import Swipeable from './components/Swipeable'
import SwipeItem from './components/SwipeItem'
import SwipeToRevealList from './components/SwipeToRevealList'

import SwipeToRevealOptions from './components/SwipeToRevealOptions'

describe('LeftRightButton', () => {
	it('is truthy', () => {
		expect(LeftRightButton).toBeTruthy()
	})
})

describe('Swipeable', () => {
	it('is truthy', () => {
		expect(Swipeable).toBeTruthy()
	})
})

describe('SwipeItem', () => {
	it('is truthy', () => {
		expect(SwipeItem).toBeTruthy()
	})
})

describe('SwipeToRevealOptions', () => {
	it('is truthy', () => {
		expect(SwipeToRevealOptions).toBeTruthy()
	})
})
describe('SwipeToRevealList', () => {
	it('is truthy', () => {
		expect(SwipeToRevealList).toBeTruthy()
	})
})
